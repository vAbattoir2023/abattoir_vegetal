<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>{% block title %}Welcome!{% endblock %}</title>
		<link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
		{# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
		
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Gasoek+One&family=Work+Sans:ital,wght@0,300;0,400;0,600;0,800;1,400;1,600&display=swap" rel="stylesheet"> 
		<link rel="stylesheet" href="{{ asset('./assets/css/stylecommun.css') }}">
        {% block stylesheets %}{% endblock %}
	</head>
		<body>
			<div class="container">
				<nav class="d-inline-flex mt-2 mt-md-0 ms-md-auto">
                {# {% if app.user  %}
				    {% if 'ROLE_ADMIN' in app.user %} #}
                    <a class="me-3 py-2 link-body-emphasis text-decoration-none" href="{{ path('app_admin_index') }}">admin</a>
                    {# {% endif %} #}
					<a class="me-3 py-2 link-body-emphasis text-decoration-none" href="{{ path('app_register') }}">Inscription</a>
					<a class="me-3 py-2 link-body-emphasis text-decoration-none" href="{{ path('app_login') }}">Connexion</a>
                    {# {% else %} #}
				    <a class="me-3 py-2 link-body-emphasis text-decoration-none" href="{{ path('app_logout') }}">Déconnexion</a>
                    <a class="me-3 py-2 link-body-emphasis text-decoration-none" href="{{ path('app_user_profil') }}">EditProfil</a>
                    <a class="me-3 py-2 link-body-emphasis text-decoration-none" href="{{ path('app_user_profil_success') }}">Profil</a>
				    {# {% endif %} #}
				</nav>
			</div>
			<div class="container"> 
                {% block body %}
                {% endblock %}
			</div>
		</body>
</html>
<script src="{{ asset('./assets/js/form.js')}}"></script>
{% block javascripts %}{% endblock %}
