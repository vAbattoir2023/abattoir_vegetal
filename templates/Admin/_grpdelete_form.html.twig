<form method="post" action="{{ path('app_admin_delete', {'id': groups.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ groups.id) }}">
    <button class="btn btn-danger mt-1">Delete</button>
</form>
